[logging]
retention_days = 30
max_log_size = 10485760

[tally]
default_timeout = 30
; log_dir: set automatically by the installer based on user's choice.
; Leave empty to fall back to the "logs" folder inside the install directory.
log_dir =
log_retention_days = 30

[sync]
batch_size = 100
retry_attempts = 3

[database]
connection_timeout = 30
